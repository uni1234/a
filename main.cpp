#include <stdio.h>

int main() {
  int n, m, k;
  scanf("%d%d%d", &n, &m, &k);
  int a[m];
  for (int i = 0; i < m; i++) {
    scanf("%d", &a[i]);
  }
  for (int j = 0; j < k; j++) {
    for (int l = 0; l < n; l++) {
      if (a[j + l + １]) }
  }
}